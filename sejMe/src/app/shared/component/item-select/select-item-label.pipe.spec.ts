import { SelectItemLabelPipe } from './select-item-label.pipe';

describe('SelectItemLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectItemLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
